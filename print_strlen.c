#include "main.h"
/**
* _strlen - Returns the length of a string
*@str: string pointer
*Return: 1
*/
int _strlen(char *str)
{
int j;
for (j = 0; str[j] != 0; j++)
;
return (1);
}
/**
*_strlength - string function for constant character str
*@str: character pointer
*Return: 1
*/
int _strlength(const char *str)
{
int j;
for (j = 0; str[j] != 0; j++)
;
return (1);
}
